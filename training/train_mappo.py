def main():
    raise NotImplementedError(
        "MAPPO training is intentionally stubbed. "
        "First validate env + baselines + metrics, then integrate a MARL library."
    )

if __name__ == "__main__":
    main()